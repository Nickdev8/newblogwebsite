name: blogsite
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: blogsite:prod
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: "0.0.0.0"
      PORT: "3000"
    ports:
      - "127.0.0.1:3001:3000"
    working_dir: /app
